<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!-- Allow the app to access the network (required for S3/SFTP/etc.) -->
  <key>com.apple.security.network.client</key>
  <true/>
  <!-- Allow reading/writing arbitrary files the user picks or that the app manages -->
  <key>com.apple.security.files.user-selected.read-write</key>
  <true/>
  <!-- Allow access to all files (needed for a file manager that browses the whole filesystem) -->
  <key>com.apple.security.files.all</key>
  <true/>
  <!-- Required for V8's JIT to allocate executable memory under hardened runtime —
       without this, Electron crashes with EXC_BREAKPOINT at launch on Apple Silicon -->
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <!-- Allow child processes (e.g. rclone, qlmanage, du) -->
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>
</dict>
</plist>
